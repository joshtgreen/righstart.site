# Git Workflow

- **`main`** is the production branch. Vercel deploys the live site from `main`. Do not push to `main` unless Josh says to.
- **`test`** is the default working branch. Always push here unless told otherwise.
- When Josh approves, merge `test` into `main`.
- This is a low-stakes personal project where iteration speed is the priority.
